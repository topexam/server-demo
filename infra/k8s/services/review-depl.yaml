apiVersion: apps/v1
kind: Deployment
metadata:
  name: review-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: review
  template:
    metadata:
      labels:
        app: review
    spec:
      containers:
        - name: review
          image: docker.io/imfelixng/topexam.svc-review:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: NATS_CLIENT_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: NATS_URL
              value: "http://nats-streaming-svc:4222"
            - name: NATS_CLUSTER_ID
              value: "topthithu"
            - name: PORT
              value: "5000"
            - name: SENTRY_DSN
              value: "https://dd6aa68b43d64126b756f6edd32111de@o496273.ingest.sentry.io/5570389"
            - name: MONGO_URI
              value: "mongodb://review-mongo-svc"
            - name: MONGO_DB_NAME
              value: "review"
            - name: AUTH0_ISSUER_URL
              value: "https://topthithu.auth0.com/"
            - name: AUTH0_AUDIENCE
              value: "https://topthithu.dev"
---
apiVersion: v1
kind: Service
metadata:
  name: review-svc
spec:
  selector:
    app: review
  ports:
    - name: review
      protocol: TCP
      port: 5000
      targetPort: 5000
