apiVersion: apps/v1
kind: Deployment
metadata:
  name: review-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: review-mongo
  template:
    metadata:
      labels:
        app: review-mongo
    spec:
      containers:
        - name: review-mongo
          image: mongo
          volumeMounts:
            - name: review-mongo-volume
              mountPath: /data/db
      volumes:
        - name: review-mongo-volume
          persistentVolumeClaim:
            claimName: review-mongo-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: review-mongo-svc
spec:
  selector:
    app: review-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
